name: CI  # Workflow name (shows in Actions tab)

on:
  push:      # Runs on code push
    branches: [ main ]  # Your main branch
  pull_request:  # Runs on PRs
    branches: [ main ]
  workflow_dispatch:  # Manual trigger button

jobs:
  test:  # Job name
    runs-on: ubuntu-latest  # Free Linux machine
    steps:
      - name: Checkout code  # Step 1: Get your code
        uses: actions/checkout@v4
        
      - name: Run tests  # Step 2: Fake test (always passes)
        run: echo "✅ All tests passed!"
